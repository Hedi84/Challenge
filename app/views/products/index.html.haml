.my-flex
  %h1 Products
  %p Click a product to view it or #{link_to "add a new product", new_product_path}
  .my-row
    - @products.each do |product|
      .my-box
        %h4
          = link_to product.title, show_path(product.id)
          = link_to "X", delete_path(product), method: :delete, data: { confirm: 'Are you sure you want to delete this product?'}
        %p
          This product currently belongs to #{product.matches.count} categories

